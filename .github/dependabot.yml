version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current (Dependabot updates the pins + comments).
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
